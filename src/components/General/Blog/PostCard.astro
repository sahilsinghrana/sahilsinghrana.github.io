---
const { title, description, image, pubDate, slug } = Astro.props;

import "./postcard.scss";
---

<a href={"/blog/posts/" + slug} class="blogPostCard">
  <img src={image.url} alt={image.alt} />
  <div class="content">
    <h3>{title}</h3>
    <p class="description">{description}</p>
    <!-- <div class="meta">
      <>
        <span>{pubDate}</span>
        <span>{author}</span>
      </>
    </div> -->
  </div>
</a>
